# Language server

Python WebSocket language model server (`lm.py`), Poetry project metadata, and related scripts.

From the repository root:

```sh
pip install -r language_server/requirements.txt
python language_server/lm.py
```

See the [root README](../README.md) for the full local setup.
