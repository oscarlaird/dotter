% !TEX root = ../math.tex
\chapter{The Bayesian Trie}
\label{chap:trie}

Dotter must determine the most likely string that the user is typing, based 
on observation of the user's signals.
To accomplish this, Dotter maintains a trie indicating the posterior probability
that any given prefix is a prefix of the user's target string.

To compare strings, we use the inequality notation $X \leq Y$ to mean that the string $X$ is a prefix of the string $Y$.

Letting $X$ be the user's target string, $D$ be the user's signals, then the posterior probability 
that a given string, $S$, is a prefix of $X$ is given by:
$$
P(S \leq X | D) = \frac{P(D | S \leq X) P(S \leq X)}{P(D)}
$$

In practice, we only keep track of the unnormalized posterior probabilities, denoted in code as \texttt{post\_Z} (to mean the posterior probability multiplied by a normalizing constant).
Since the root node, $S_0$, is the empty string, we have $P(S_0 \leq X) = 1$ 
and thus the unnormalized posterior probability of the root node is $P(D)$, i.e., the normalizing constant.
Thus if we know the unnormalized posterior probabilities in our trie, we can convert these to normalized
posterior probabilities by dividing by the root node's unnormalized posterior probability.

The posterior probabilities are derived from the prior probabilities and the likelihoods.
In Dotter, both are subject to updates. The likelihood is updated as we receive signals from the user
and the prior is updated as our language model refines our predictions.

Our analysis has five parts:
\begin{enumerate}
    \item We assume a character-based prior model, present algorithms for lazy
    calculation of the unnormalized posteriors, and prove their correctness.
    \item We discuss implementation details with regard to tracking the likelihoods.
    \item We discuss how to adapt the algorithms for a token-based prior model.
    \item We discuss how to track the maximum descendant likelihoods of token sequences.
    \item We present the full algorithms.
\end{enumerate}


\section{Lazy Posterior Calculation: The Core Algorithms}

\begin{definition}[Alphabet]
An alphabet is a finite set.
\end{definition}

\begin{definition}[Character]
A character is an element of an alphabet.
\end{definition}

\begin{definition}[String]
A string is a sequence of characters.
\end{definition}

\begin{definition}[Substring]
We use the notation $s[i:j]$ to represent the subsequence of characters of $s$ from index $i$ to index $j-1$.
We also allow the notation $s[:-k]$ to represent the subsequence of characters of $s$ from index 0 to index $|s|-k-1$.
\end{definition}

\begin{definition}[String Inequality]
We say that a string $s$ is a prefix of a string $t$, and write this as $s \leq t$, if and only if there exists an integer $k$ such that $s = t[0:k]$.
\end{definition}

\begin{definition}[Properly Terminated String]
A string $h$ is properly terminated if and only if $h[i]=\text{\textquotesingle}\blacksquare\text{\textquotesingle} \iff i=|h|-1$.
An unterminated string is one that does not contain the stop character.
An ``at most properly terminated string'' is one that is either properly terminated or unterminated.
\end{definition}

Throughout this chapter, we will let $\mathcal{A}$ denote our alphabet which must include at least
the two special characters: space ($\text{\textquotesingle}\;\text{\textquotesingle}$) and stop ($\text{\textquotesingle}\blacksquare\text{\textquotesingle}$); the set of all strings over
this alphabet will be denoted $S$, and the subset of all properly terminated strings will be denoted
$H$, where if $h \in H$.

\begin{definition}[Prefix Code]
    A prefix code is a set of strings such that no string is a prefix of another string in the set, i.e.,
    A set $C$ is a prefix code if and only if,
    $$
    \not\exists c_1, c_2 \in C, c_1 < c_2
    $$
\end{definition}

\begin{definition}[Complete Prefix Code]
    A complete prefix code, $C$, is a prefix code that covers the entire tree, i.e.,
    $$
    \forall x \in S, \exists c \in C, x \leq c \lor c \leq x
    $$
\end{definition}

\begin{definition}[Refinement]
    A prefix code, $A$, is a refinement of a prefix code, $B$,
    if every element of $A$ is an extension of some element of $B$, i.e.,
    $$
    \forall a \in A, \exists b \in B, a \geq b
    $$
    If this is the case, we write $A \geq B$.
\end{definition}

Refinement is a partial order over complete prefix codes.
These prefix codes form a lattice, so we may define the meet
and join of two prefix codes.

\begin{definition}[Meet]
    The meet of two prefix codes, $A$ and $B$, is the greatest lower bound of $A$ and $B$, i.e.,
    i.e.,
    it is the set of minimal elements of their intersection.
    We write this as $A \wedge B$.
\end{definition}

\begin{definition}[Join]
    The join of two prefix codes, $A$ and $B$, is the least upper bound of $A$ and $B$, i.e.,
    it is the set of maximal elements of their union.
    We write this as $A \vee B$.
\end{definition}

\begin{definition}[Element Refinement]
    We overload the comparison operators when the left hand side is a string
    and the right hand side is a complete prefix code.
    $x \leq C$ means that $x$ is a prefix of some element of $C$,
    whereas $x > C$ means that $x$ is a proper extension of some element of $C$.
\end{definition}

\begin{proposition}
    The set of all properly terminated strings, $H$, is a complete prefix code.
\end{proposition}
\begin{proof}
    We first show that $H$ is a prefix code.
    We proceed by contradiction.
    Suppose there exist $h_1, h_2 \in H$ such that $h_1 < h_2$.
    Because $h_1$ is a strict prefix of $h_2$, it must be that $|h_1| < |h_2|$.
    Furthermore, the characters of $h_2$ must perfectly match $h_1$ up to index $|h_1|-1$. 
    Thus, 
    $$
    h_2[|h_1|-1] = h_1[|h_1|-1].
    $$
    Because $h_1 \in H$, it is properly terminated, meaning its final character is the stop character: $h_1[|h_1|-1] = \text{\textquotesingle}\blacksquare\text{\textquotesingle}$.
    Therefore, $h_2[|h_1|-1] = \text{\textquotesingle}\blacksquare\text{\textquotesingle}$.
    However, because $h_2 \in H$, the stop character can only appear at its final index $|h_2|-1$. This implies:
    $$
    |h_1|-1 = |h_2|-1 \implies |h_1| = |h_2|,
    $$
    which contradicts the strict prefix requirement $|h_1| < |h_2|$. Thus, no such pair $h_1, h_2$ can exist, and $H$ is a prefix code.


    We now show that $H$ is a complete prefix code.
    Let $x \in S$ be any string.
    We show this by cases constructively:
    \begin{enumerate}[label=(\alph*)]
        \item If $\text{\textquotesingle}\blacksquare\text{\textquotesingle}$ is in $x$,
        then let $i$ be the index of its first occurrence. Then $x[:i+1] \in H$
        and $x[:i+1] \leq x$.
        \item If $\text{\textquotesingle}\blacksquare\text{\textquotesingle}$ is not in $x$,
        then $x+\text{\textquotesingle}\blacksquare\text{\textquotesingle} \in H$
        and $x < x+\text{\textquotesingle}\blacksquare\text{\textquotesingle}$.
    \end{enumerate}
\end{proof}

\begin{definition}[Likelihood Function]
    A likelihood function is a mapping from properly terminated strings to non-negative real numbers,
    $L_n : H \to \mathbb{R}^+$.
\end{definition}

\begin{definition}[Prior Function]
\label{def:prior-function}
    A prior function is a mapping from properly terminated strings to non-negative real numbers,
    $P_m : H \to \mathbb{R}^+$,
    satisfying
    $$
    \sum_{h \in H} P_m(h) = 1
    $$
\end{definition}

\begin{definition}[Branch Prior Function]
\label{def:branch-prior-function}
    We define the branch prior function, $P^{br}_m(x)$, of an at most properly terminated string $x$,
    as,
    $$
    P^{br}_m(x) = \sum_{\substack{h \in H \\ h \geq x}} P_m(h)
    $$
\end{definition}
\textit{Terminology Note: } The branch prior function corresponds to our usual notion of a prior over
string prefixes, but we choose to name it explicitly as ``branch prior'' and to restrict our definition of the domain of
a ``prior function'' to properly terminated strings,
excluding their prefixes. We make this choice to keep a close correspondence
between the definitions of prior and likelihood functions.

\textit{Note: } In our algorithm, we will assume oracular access to the branch prior function.

\begin{definition}[Likelihood Sequence]
\label{def:likelihood-sequence}
    A likelihood sequence is a sequence of likelihood functions,
    $\{L_n\}$ where $\forall h, L_0(h) = 1$.
\end{definition}

\begin{definition}[Prior Sequence]
    A prior sequence is a sequence of prior functions $\{P_n\}$.
\end{definition}

\begin{definition}[Delta Digest]
    A delta digest is a prefix code and a mapping from that prefix code to non-negative real numbers, $(C, \Delta)$.
    So,
    $$
    \Delta : C \to \mathbb{R}^+
    $$
\end{definition}

\begin{definition}[Prefix Code Truncation]
    The truncation of a string $x$ under a prefix code $C$ refers to the
    element $c \in C$ such that $c \leq x$.
    If no such element exists, then the truncation is undefined.
    We denote the truncation of $x$ under $C$ as $\lfloor x \rfloor_C$.
\end{definition}

\begin{definition}[Likelihood Delta Digest Sequence]
    The likelihood delta digest sequence corresponding to a likelihood sequence $\{L_n\}$ is a sequence of delta digests $\{(C^L_n, \Delta^L_n)\}$,
    that satisfies:
    $$
    \forall h \in H, L_n(h) = L_{n-1}(h) \Delta^L_n(\lfloor h \rfloor_{C^L_n})
    $$
\end{definition}

\begin{definition}[Prior Delta Digest Sequence]
    This is defined identically,
    i.e.,
    the prior delta digest sequence corresponding to a prior sequence $\{P_n\}$ is a sequence of delta digests $\{(C^P_n, \Delta^P_n)\}$,
    that satisfies:
    $$
    \forall h \in H, P_n(h) = P_{n-1}(h) \Delta^P_n(\lfloor h \rfloor_{C^P_n})
    $$
\end{definition}

\begin{lemma}[Eventually Constant Likelihoods]
\label{lemma:eventually-constant-likelihoods}
    Given the cumulative likelihood update frontier, $\bigvee_{i=1}^N C^L_i$,
    and a given node in that frontier, $f$,
    the likelihoods of all properly terminated strings
    descended from that node are constant with respect to the branch up to time $N$, i.e.,
    $$
    \forall n \leq N, \forall h \in H, h \geq f, L_n(h) = \prod_{1\leq i \leq n} \Delta^L_i(\lfloor f \rfloor_{C^L_i})
    $$
\end{lemma}
\begin{proof}
    Recall that $L_0(h) = 1$. By definition of the likelihood sequence, 
    $$
    L_n(h) = \prod_{1 \leq i \leq n} \Delta^L_i(\lfloor h \rfloor_{C^L_i}).
    $$
    We must show that $\lfloor h \rfloor_{C^L_i} = \lfloor f \rfloor_{C^L_i}$ for all $1\leq i \leq n$.
    
    Any element $f \in \bigvee_{i=1}^N C^L_i$ is a descendant of or equal to an element in each $C^L_i$. In other words, for every $i \in \{1, \dots, n\}$, there exists some $c_i \in C^L_i$ such that $c_i \leq f$. Because $c_i$ is a prefix of $f$, it is exactly the truncation of $f$ under $C^L_i$, so $c_i = \lfloor f \rfloor_{C^L_i}$.

    By hypothesis, $h \geq f$. Since the prefix relation is transitive, $c_i \leq f$ and $f \leq h$ imply $c_i \leq h$. Because $C^L_i$ is a prefix code, $c_i$ is the unique element in $C^L_i$ that is a prefix of $h$. 
    Therefore, $\lfloor h \rfloor_{C^L_i} = c_i = \lfloor f \rfloor_{C^L_i}$.
    Substituting this into the product yields the result.
\end{proof}

\begin{definition}[Unnormalized Posterior Function]
    The unnormalized posterior function is a mapping
    from at most properly terminated strings to non-negative real numbers given by
    $$
    Z_{n,m}(x) = \sum_{\substack{h \in H \\ h \geq x}} L_n(h) P_m(h)
    $$
\end{definition}

\begin{proposition}
    Without any likelihood updates, the unnormalized posterior of the root node is 1:
    \begin{align*}
        Z_{0,m}(\text{\textquotesingle}\text{\textquotesingle}) &= \sum_{\substack{h \in H \\ h \geq \text{\textquotesingle}\text{\textquotesingle}}} L_0(h) P_m(h) \notag \\
        &= \sum_{h \in H} L_0(h) P_m(h) \notag \\
        &= \sum_{h \in H} 1 \cdot P_m(h) && \text{(by definition \ref{def:likelihood-sequence})} \\
        &= 1 && \text{(by definition \ref{def:prior-function})} \notag
    \end{align*}
\end{proposition}

\begin{lemma}[Posterior Equals Sum of Children]
\label{lemma:posterior-equals-sum-of-children}
    if $x$ is an unterminated string, then
    $$
    Z_{n,m}(x) = \sum_{a \in \mathcal{A}} Z_{n,m}(x+a)
    $$
\end{lemma}
\begin{proof}
    By definition,
    $$
    Z_{n,m}(x) = \sum_{\substack{h \in H \\ h \geq x}} L_n(h) P_m(h).
    $$
    Since $x$ is an unterminated string, it does not contain the stop character. Because all strings in $H$ are properly terminated and therefore end with the stop character, $x \notin H$. Thus, any properly terminated string $h \in H$ that has $x$ as a prefix must be strictly longer than $x$ ($|h| > |x|$).

    Therefore, the character in $h$ immediately following the prefix $x$ must be some character $a \in \mathcal{A}$. We can partition the set of strings $\{h \in H \mid h \geq x\}$ into mutually disjoint subsets based on this next character $a$:
    $$
    \{h \in H \mid h \geq x\} = \bigcup_{a \in \mathcal{A}} \{h \in H \mid h \geq x+a\}.
    $$
    Because these subsets are disjoint, we can split the summation over them:
    \begin{align*}
        Z_{n,m}(x) &= \sum_{a \in \mathcal{A}} \sum_{\substack{h \in H \\ h \geq x+a}} L_n(h) P_m(h) \\
        &= \sum_{a \in \mathcal{A}} Z_{n,m}(x+a).
    \end{align*}
\end{proof}

We may now introduce the datastructure at the core of Dotter, the Bayesian Trie.
The Bayesian Trie maintains a calculation of the unnormalized posterior probability
while allowing for the lazy application of likelihood and prior updates.

\begin{definition}[Bayesian Trie Node]
    A Bayesian trie node $x$ associated with a string $s \in S$ is a data structure containing the tuple $(n, m, Z)$, accessed via fields:
    \begin{itemize}
        \item $x.n \in \mathbb{N}$, the ``likelihood time'' of the node.
        \item $x.m \in \mathbb{N}$, the ``prior time'' of the node.
        \item $x.Z \in \mathbb{R}^+$, a computed quantity which should be equal to the unnormalized posterior of the node evaluated at those times, i.e., $x.Z = Z_{x.n, x.m}(s)$.
    \end{itemize}
\end{definition}

\begin{definition}[Valid Node]
    A node, $x$, is called valid at time ($N$, $M$)
    if it satisfies one of the following two conditions:
    \begin{enumerate}
        \item \textbf{Constant Likelihood}: $x$ lies beyond
        the cumulative likelihood update frontier, i.e.,
        $x \geq \bigvee_{i=1}^N C^L_i$
        \item \textbf{No Reweighting}:
        The likelihood and the prior have changed by a constant factor
        since the node's time, i.e.,
        $x \geq \bigvee_{i=x.n+1}^N C^L_i$ and $x \geq \bigvee_{i=x.m+1}^M C^P_i$
    \end{enumerate}
\end{definition}

\begin{lemma}[Current Nodes are Valid]
\label{lemma:current-nodes-are-valid}
    A node is always valid at its own time.
    I.e.,
    $(x.n, x.m) = (N, M)$ implies that $x$ is valid at time $(N, M)$.
\end{lemma}
\begin{proof}
    Such a node satisfies the ``No Reweighting'' condition
    of validity. Because $(x.n, x.m) = (N, M)$, the two 
    joins are joins over empty sets and thus equal to the root node.
    Hence,
        $x \geq \bigvee_{i=x.n+1}^N C^L_i$ and $x \geq \bigvee_{i=x.m+1}^M C^P_i$
    .
\end{proof}

\begin{definition}[Bayesian Trie]
    A Bayesian Trie is a trie of Bayesian trie nodes
    along with a global tuple describing the current time,
    $(N, M)$.
\end{definition}

\begin{definition}[Valid Bayesian Trie]
    A Bayesian Trie is called valid, if all of its nodes are valid
    for the trie's global current time, $(N, M)$.
\end{definition}

\begin{definition}[Node Correctness]
    A node is ``correct'' if it correctly calculates the unnormalized posterior,
    $$
    x.Z = Z_{x.n, x.m}(x)
    $$
\end{definition}

\begin{definition}[Bayesian Trie Correctness]
    The Bayesian trie is ``correct'' if all of its nodes are correct.
\end{definition}

We may now give the algorithm to advance a node's time.
This algorithm recalculates the unnormalized posterior
of a valid node, $x$, to a new time, $(n', m')$.
Recall that we assume oracular access to the branch prior function, $P^{br}_m(x)$.
\begin{algorithm}[H]
\caption{Advance Node Time}
\label{alg:advance-node-time}
\begin{algorithmic}[1]
\Require $x$, the node to update.
\Require $(n, m)$, the node's current time.
\Require $(n', m')$, the new time.
\Require $(N, M)$, the trie's current time.
\Require $x$ is valid at time $(N, M)$.
\If{ $x \geq \bigvee_{i=x.n+1}^N C^L_i$ and $x \geq \bigvee_{i=x.m+1}^M C^P_i$}
    \State $x.Z \gets x.Z \prod_{i=x.n+1}^{n'} \Delta^L_i(\lfloor x \rfloor_{C^L_i}) \prod_{i=x.m+1}^{m'} \Delta^P_i(\lfloor x \rfloor_{C^P_i})$
    \label{alg:advance-node-time-no-reweighting}
\Else{ $x \geq \bigvee_{i=1}^N C^L_i$}
    \State $x.Z \gets P^{br}_{m'}(x) \prod_{i=1}^{n'} \Delta^L_i(\lfloor x \rfloor_{C^L_i})$
    \label{alg:advance-node-time-constant-likelihood}
\EndIf
\State $x.n \gets n'$
\State $x.m \gets m'$
\end{algorithmic}
\end{algorithm}

We also provide for advancing the trie to a new global time.

\begin{algorithm}[H]
\caption{Advance Trie Time}
\label{alg:advance-trie-time}
\begin{algorithmic}[1]
\Require $(N, M)$, the trie's current time.
\Require $trie$ is valid at time $(N, M)$.
\Require $(N', M')$, the new time.
\State $trie.N \gets N'$
\State $trie.M \gets M'$
\State $F \gets \bigvee_{i=1}^{N'} C^L_i \wedge \left(\bigvee_{i=N+1}^{N'} C^L_i \vee \bigvee_{i=M+1}^{M'} C^P_i\right)$
\Comment $x \geq F$ implies $x$ is valid at time $(N', M')$.
\State Run \texttt{AdvanceNodeTime} to update the time of
each node in this frontier to $(N', M')$.
\For{each node $x$ strictly within $F$, $x < F$, proceeding in reverse topological order}
    \State $x.Z \gets \sum_{y \in x.\text{children}} y.\text{Z}$
    \label{alg:advance-trie-time-recalc-up-propagation}
    \State $x.n \gets N'$
    \State $x.m \gets M'$
\EndFor
\end{algorithmic}
\end{algorithm}

We now show that both algorithms preserve
the validity of the trie and its correctness.

\begin{theorem}[Advance Node Time Validity]
    The \texttt{AdvanceNodeTime} algorithm preserves the validity of the node.
\end{theorem}
\begin{proof}
    The ``constant likelihood'' depends only on the global time,
    so updating the node's time does not affect if this condition is satisfied.
    The ``no reweighting'' condition is weakened by increasing the node's time.
    I.e.,
    $$
    x \geq \bigvee_{i=n+1}^N C^L_i \implies x \geq \bigvee_{i=n'+1}^{N} C^L_i
    $$
    and,
    $$
    x \geq \bigvee_{i=m+1}^M C^P_i \implies x \geq \bigvee_{i=m'+1}^{M} C^P_i
    $$
    if $n' \geq n$.
\end{proof}

\begin{theorem}[Advance Node Time Correctness]
    The \texttt{AdvanceNodeTime} algorithm preserves the correctness of the node.
\end{theorem}
\begin{proof}
    We show this for the two cases of valid nodes.

    \textbf{Constant Likelihood Case:} If $x$ satisfies,
    $x \geq \bigvee_{i=1}^N C^L_i$, 
    then,
    \begin{align*}
    Z_{n',m'}(x) &= \sum_{\substack{h \in H \\ h \geq x}} P_{m'}(h) L_{n'}(h) \\
    &= \sum_{\substack{h \in H \\ h \geq x}} P_{m'}(h) \prod_{i=1}^{n'} \Delta^L_i(\lfloor h \rfloor_{C^L_i}) \\
    &= \sum_{\substack{h \in H \\ h \geq x}} P_{m'}(h) \prod_{i=1}^{n'} \Delta^L_i(\lfloor x \rfloor_{C^L_i}) && \text{by lemma \ref{lemma:eventually-constant-likelihoods}} \\
    &= P^{br}_{m'}(x) \prod_{i=1}^{n'} \Delta^L_i(\lfloor x \rfloor_{C^L_i}) && \text{by definition \ref{def:branch-prior-function}}
    \end{align*}
    which is accomplished by line \ref{alg:advance-node-time-constant-likelihood}.

    \textbf{No Reweighting Case:} If $x$ satisfies,
    $x \geq \bigvee_{i=x.n+1}^N C^L_i$ and $x \geq \bigvee_{i=x.m+1}^M C^P_i$,
    then,
    \begin{align*}
    Z_{n',m'}(x) &= \sum_{\substack{h \in H \\ h \geq x}} P_{m'}(h) L_{n'}(h) \\
    &= \sum_{\substack{h \in H \\ h \geq x}} \left( L_{n}(h) \prod_{i=x.n+1}^{n'} \Delta^L_i(\lfloor h \rfloor_{C^L_i}) \right) \left( P_m(h) \prod_{i=x.m+1}^{m'} \Delta^P_i(\lfloor h \rfloor_{C^P_i}) \right) \\
    &= \sum_{\substack{h \in H \\ h \geq x}} L_{n}(h) P_m(h) \prod_{i=x.n+1}^{n'} \Delta^L_i(\lfloor h \rfloor_{C^L_i}) \prod_{i=x.m+1}^{m'} \Delta^P_i(\lfloor h \rfloor_{C^P_i}) \\
    &= Z_{n,m}(x) \prod_{i=x.n+1}^{n'} \Delta^L_i(\lfloor x \rfloor_{C^L_i}) \prod_{i=x.m+1}^{m'} \Delta^P_i(\lfloor x \rfloor_{C^P_i})
    \end{align*}
    which is accomplished by line \ref{alg:advance-trie-time-recalc-up-propagation}.
\end{proof}

\begin{lemma}[Frontier Calculation]
\label{lemma:frontier-calculation}
    If $F = \bigvee_{i=1}^{N'} C^L_i \wedge \left(\bigvee_{i=N+1}^{N'} C^L_i \vee \bigvee_{i=M+1}^{M'} C^P_i\right)$,
    then $x \geq F$ and $x$ is valid at time $(N, M)$ imply $x$ is valid at time $(N', M')$.
\end{lemma}
\begin{proof}
    If $x \geq F$, then either, $x \geq \bigvee_{i=1}^{N'} C^L_i$ or $x \geq \bigvee_{i=N+1}^{N'} C^L_i \vee \bigvee_{i=M+1}^{M'} C^P_i$.
    The first case is exactly the same as the ``Constant Likelihood'' branch in the definition of validity at time $(N', M')$.
    So we only need to consider the second case where $x \geq \bigvee_{i=N+1}^{N'} C^L_i \vee \bigvee_{i=M+1}^{M'} C^P_i$.

    The other assumption, that $x$ is valid at time $(N, M)$, also has two cases: either,
    $x \geq \bigvee_{i=1}^N C^L_i$ or $x \geq \bigvee_{i=x.n+1}^N C^L_i \vee \bigvee_{i=x.m+1}^M C^P_i$.

    \textbf{Case 1: } If $x \geq \bigvee_{i=1}^N C^L_i$ and $x \geq \bigvee_{i=N+1}^{N'} C^L_i \vee \bigvee_{i=M+1}^{M'} C^P_i$,
    then we may combine the two joins over the likelihood frontiers to write,
    $x \geq \bigvee_{i=1}^{N'} C^L_i$, which is the ``Constant Likelihood'' branch
    for validity at time $(N', M')$.

    \textbf{Case 2: } If $x \geq \bigvee_{i=x.n+1}^N C^L_i \vee \bigvee_{i=x.m+1}^M C^P_i$ and $x \geq \bigvee_{i=N+1}^{N'} C^L_i \vee \bigvee_{i=M+1}^{M'} C^P_i$,
    then we may combine the joins over the likelihood and prior frontiers to write,
    $x \geq \bigvee_{i=x.n+1}^{N'} C^L_i \vee \bigvee_{i=x.m+1}^{M'} C^P_i$, which is the ``No Reweighting'' branch
    for validity at time $(N', M')$.
\end{proof}

\begin{theorem}[Advance Trie Time Validity]
    We must show that after running \texttt{AdvanceTrieTime},
    the trie is valid at its new time, $(N', M')$.
\end{theorem}
\begin{proof}
    By lemma \ref{lemma:frontier-calculation}, if $x \geq F$, then $x$ is valid at the new time.
    Otherwise, $x$ is strictly within $F$. All such nodes
    are updated to have the new time $(x.n, x.m) = (N', M')$.
    By lemma \ref{lemma:current-nodes-are-valid}, these nodes are valid at their new time.
\end{proof}

\begin{theorem}[Advance Trie Time Correctness]
    The \texttt{AdvanceTrieTime} algorithm preserves the correctness of the trie.
\end{theorem}
\begin{proof}
    We show this by induction on the trie.

    We are concerned with nodes, $x \leq F$, since nodes
    which are beyond the update boundary, $x > F$, are unaffected
    by the algorithm.

    At the start of the algorithm, all nodes are valid and correct.
    
    \textbf{Base Case: } If $x \in F$, the $x$ is valid and correct
    and we have already shown that running \texttt{AdvanceNodeTime} on $x$ preserves its validity and correctness.

    \textbf{Induction Step: } If $x < F$, then, because
    we process nodes in reverse topological order, by the induction hypothesis,
    all its immediate children are correct and have time $(N', M')$.
    Thus line \ref{alg:advance-trie-time-recalc-up-propagation} recalculates the unnormalized posterior of $x$ correctly
    by lemma \ref{lemma:posterior-equals-sum-of-children}.
\end{proof}



\section{Likelihood Tracking}
The preceding algorithm is correct, but we wish to avoid keeping all the
$\Delta^L_i$ in memory globally and the cost of calculating truncations, $\lfloor x \rfloor_{C^L_i}$.
To do so we introduce a field called ``unpushed likelihood'' which will be used 
to track the product of likelihood deltas.
Furthermore, we limit ourselves to handling a single likelihood update at a time.

Formally, we define augmented nodes with an $x.l$ field, describining the unpushed likelihood.
We define an augmented trie corresponding to these nodes.

\begin{algorithm}[H]
\caption{Single Likelihood Update}
\label{alg:single-likelihood-update}
\begin{algorithmic}[1]
\Require Bayesian Trie $B$
\Require $B$ is valid at time $(N, M)$.
\For{each node $x < C^L_{N+1}$ in topological order}
    \State $y.l \gets y.l * x.l  \quad \forall y \in x.\text{children}$
    \label{alg:single-likelihood-update-downprop-1}
    \State $x.l \gets 1$
    \label{alg:single-likelihood-update-downprop-2}
\EndFor
\Comment{For each $x \in C^L_{N+1}$, the product $\prod_{i=x.n+1}^{N} \Delta^L_i(\lfloor x \rfloor_{C^L_i})$ is the unpushed likelihood, $x.l$.}
\For{each node $x \in C^L_{N+1}$}
    \State $x.l \gets x.l * \Delta^L_{N+1}(x)$
    \label{alg:single-likelihood-update-create-unpushed-likelihood}
\EndFor
\State Run \texttt{AdvanceTrieTime} to the new time of $(N+1, M)$.
\For{each node $x \in C^L_{N+1}$}
    \State $y.l \gets y.l * x.l \quad \forall y \in x.\text{children}$
    \label{alg:single-likelihood-update-final-downprop-1}
    \State $x.l \gets 1$
    \label{alg:single-likelihood-update-final-downprop-2}
\EndFor
\State $B.N \gets B.N+1$
\end{algorithmic}
\end{algorithm}

We define our invariant, ``push correctness'':

\begin{definition}[Push Correctness]
\label{def:push-correctness}
    We say that a Bayesian trie $B$ is push correct for time $(N, M)$ if,
    for every node whose descendant likelihoods have changed by a constant factor,
    that factor is equal to the product of the ``unpushed likelihood'' fields of all of its ancestors (including itself).
    I.e.,
    \[
    \forall x,\, x \geq \bigvee_{i=x.n+1}^{N} C^L_i \implies \prod_{y \leq x} y.l = \prod_{i = x.n+1}^{N} \Delta^L_i\bigl(\lfloor x \rfloor_{C^L_i}\bigr)
    \]
\end{definition}

\begin{theorem}[Single Likelihood Update Push Correctness]
\label{theorem:single-likelihood-update-push-correctness}
    The \texttt{SingleLikelihoodUpdate} algorithm preserves push correctness.
\end{theorem}
\begin{proof}
    There are two cases to consider: $x \leq C^L_{N+1}$ and $x > C^L_{N+1}$.
    For $x \leq C^L_{N+1}$, push correctness is trivially satisfied. 
    By the end of the algorithm, such a node's local time has been updated to $x.n = N+1$ 
    (during the call to \texttt{AdvanceTrieTime}), making the empty product on the right-hand side equal to 1. 
    Correspondingly, its unpushed likelihood $x.l$ has been set to 1,
    leaving the left-hand side product equal to 1.

    If $x > C^L_{N+1}$, we rely on the inductive assumption that the trie was push correct
    at the start of the algorithm.
    In this case, lines~\ref{alg:single-likelihood-update-downprop-1} and~\ref{alg:single-likelihood-update-downprop-2}
    do not affect the push correctness for these nodes since unpushed likelihood
    is simply moved from one ancestor to another, leaving the total product $\prod_{y \leq x} y.l$ unchanged.
    On line~\ref{alg:single-likelihood-update-create-unpushed-likelihood},
    we transition from being correct for time $(N, M)$ to being correct for time $(N+1, M)$.
    For every node $x > C^L_{N+1}$, we correctly multiply the factor $\Delta^L_{N+1}(\lfloor x \rfloor_{C^L_{N+1}})$ 
    into the unpushed likelihood of the ancestor node in $C^L_{N+1}$.
    Finally, on lines~\ref{alg:single-likelihood-update-final-downprop-1} and~\ref{alg:single-likelihood-update-final-downprop-2},
    we once again merely redistribute the unpushed likelihood among ancestors without changing the total product.
\end{proof}

\begin{lemma}[Equivalence of Unpushed Likelihood]
\label{lemma:equivalence-unpushed-likelihood}
    Immediately before the call to \texttt{AdvanceTrieTime} in \texttt{SingleLikelihoodUpdate}, 
    for every node $x \in C^L_{N+1}$, the value $x.l$ is exactly equal to the historical product 
    of likelihood deltas:
    \[
    x.l = \prod_{i=x.n+1}^{N+1} \Delta^L_i\bigl(\lfloor x \rfloor_{C^L_i}\bigr)
    \]
\end{lemma}
\begin{proof}
    Before line~\ref{alg:single-likelihood-update-create-unpushed-likelihood}, the first loop has propagated all 
    unpushed likelihoods down to the nodes in $C^L_{N+1}$. By the push correctness invariant for time $N$ 
    (which applies since $x \in C^L_{N+1} \implies x \geq \bigvee_{i=x.n+1}^N C^L_i$), and because 
    all proper ancestors $y < x$ have had their $y.l$ set to 1, the value of $x.l$ is exactly 
    $\prod_{i=x.n+1}^N \Delta^L_i(\lfloor x \rfloor_{C^L_i})$. 
    Line~\ref{alg:single-likelihood-update-create-unpushed-likelihood} then multiplies $x.l$ by 
    $\Delta^L_{N+1}(\lfloor x \rfloor_{C^L_{N+1}})$, extending the product to $N+1$.
\end{proof}

\begin{corollary}[Correctness of Efficient AdvanceNodeTime]
\label{corollary:efficient-advancenodetime}
    In an efficient implementation of \texttt{AdvanceNodeTime} for a likelihood update, 
    the calculation of the new posterior $x.Z$ for nodes $x \in C^L_{N+1}$ 
    (line~\ref{alg:advance-node-time-no-reweighting} of Algorithm~\ref{alg:advance-node-time}) 
    can replace the explicit product over historical deltas with a single multiplication by $x.l$:
    \[
    x.Z \gets x.Z \cdot x.l
    \]
    Because this substitution yields the exact same mathematical value for $x.Z$ by 
    Lemma~\ref{lemma:equivalence-unpushed-likelihood}, this efficient $O(1)$ implementation 
    trivially inherits the Validity and Correctness theorems proven for the abstract \texttt{AdvanceTrieTime}.
\end{corollary}


\section{Character Priors from Token Models}

\textit{Note: } It is helpful to make three kinds of distinctions: (1) between sets and their elements,
(2) between sequences and their elements, and (3) between characters and tokens. We will choose to 
use uppercase letters for sets, Greek letters for individual characters, and bold font for token sequences.
For example, $\mathbf{A}$ would represent a set of token sequences while $\alpha$ would represent a character.

Since Dotter uses a letter-by-letter trie, but language models return token probabilities,
we face the task of converting a token model to a literal model.

A given literal prefix corresponds to several token paths.
For example, the prefix \texttt{sci} could be part of the words with the following
tokenizations (using the GPT-2 tokenizer):
\begin{itemize}
    \item \texttt{scientific} (single token),
    \item \texttt{science} (single token),
    \item \texttt{sci-ences} (two tokens),
    \item \texttt{sc-iss-or} (three tokens),
\end{itemize}
We can see that a literal prefix can correspond to multiple 
token prefixes (empty string, \texttt{sci}, and \texttt{sc}), and that for a given token prefix, the literal prefix
can be generated from multiple terminal tokens (\texttt{science}, \texttt{scientific}).

Here we introduce a formalization of tokenization and a key property
of byte-pair encoders.

\begin{definition}[Token]
    A token is a non-empty string.
\end{definition}

\begin{definition}[Token Set]
    A token set is a set of tokens.
    We will use the notation $\mathbf{\Lambda}$ to represent our token set.
\end{definition}

\begin{definition}[Stop Token]
    We will require that our token set contain a token corresponding to the ``stop'' character,
    $\text{\textquotesingle}\blacksquare\text{\textquotesingle}$.
\end{definition}

\begin{definition}[Properly Terminated Token Sequence]
    A token sequence is properly terminated if and only if it ends with the stop token, with all
    preceding tokens not being the stop token. I.e.,
    \[
    \forall i \quad (i = |\mathbf{a}| - 1 \iff \mathbf{a}[i] = \text{\textquotesingle}\blacksquare\text{\textquotesingle})
    \]
\end{definition}

Analogously to the string case, we will use the notation $\mathbf{H}$ to
represent the set of all properly terminated token sequences.

\begin{definition}[Token Sequence]
    A token sequence is a sequence of tokens.
\end{definition}

\begin{definition}[Literalization Function]
    The literalization function, $T^{-1}$, maps a token sequence to a string, by concatenating its tokens.
    I.e.,
    $T^{-1}(\mathbf{a}) = Concat_{i < |\mathbf{a}|}(\mathbf{a}[i])$.
\end{definition}

\begin{definition}[Tokenization Function]
    A tokenization function is a mapping from strings to token sequences,
    $T: \mathcal{A}^* \to \mathbf{\Lambda}^*$,
    that is inverted by literalization, i.e.,
    $$
    T^{-1}(T(x)) = x \quad \forall x \in \mathcal{A}^*
    $$
    (but the converse will not usually hold, i.e., different token sequences may correspond to the same string.)
    We also require that the tokenization function maps
    properly terminated strings to properly terminated token sequences,
    $$
    T: H \to \mathbf{H}
    $$
\end{definition}


\begin{definition}[Token to Character Index Function]
    The token to character index function is a utility function
    mapping the index of a token within its token sequence
    to the index of the first character of that token in the corresponding string.
    $$
    TokenToCharacterIndex(\mathbf{a}, i) = \sum_{j < i} |\mathbf{a}[j]|
    $$
\end{definition}

\begin{proposition}[Monotonicity of Token to Character Index Function]
    The token to character index function is strictly monotonic.
\end{proposition}
\begin{proof}
    This follows from the requirement that tokens are non-empty.
\end{proof}

\begin{definition}[Character to Token Index Function]
    The character to token index function, $CharToTokenIndex_T(x, j)$, is a utility function derived from the tokenization function, $T$,
    mapping the index of a character in the string to the index of the token that contains that character.
    I.e. it is the greatest value of $i$ that satisfies,
    $$
    TokenToCharacterIndex(T(x), i) \leq j
    $$
\end{definition}

\begin{definition}[Break Point]
    We refer to an index in a string as a ``break point'' if it is the first character of a token
    with respect to our tokenization function. So $i$ is a break point for a string $x$ and
    a tokenization function $T$ if there exists some $j$ such that $i = TokenToCharacterIndex(T(x), j)$.
\end{definition}

\begin{definition}[Boundary Sufficiency]
    The boundary sufficiency property of a tokenization function states that
    if $i_1, i_2$ are break points in a string $x$, then the tokenization within
    the interval $[i_1, i_2)$ does not depend on characters outside this interval.
    Formally, a tokenization function $T$ has boundary sufficiency if for all strings $x$
    and break points $i_1 \le i_2$ of $x$, letting
    $j_1 = CharToTokenIndex_T(x, i_1)$ and $j_2 = CharToTokenIndex_T(x, i_2)$, we have
    $$
    T(x[i_1:i_2]) = T(x)[j_1:j_2]
    $$
\end{definition}

Henceforth, we will always assume that our tokenization function has boundary sufficiency.
This is because all byte-pair encoders have this property.

We can now formalize a token-based language model.

\begin{definition}[Conditional Token Prior Function]
    Our conditional token prior function tells us the conditional probability of a token
    given a token sequence. It is a mapping
    $$
    \mathbf{P} : \mathbf{\Lambda}^* \times \mathbf{\Lambda} \to \mathbb{R}^+
    $$
    written as $\mathbf{P}(\mathbf{t} \mid \mathbf{a})$,
    where,
    $$
    \forall \mathbf{a} \in \mathbf{\Lambda}^*, \sum_{\mathbf{t} \in \mathbf{\Lambda}} \mathbf{P}(\mathbf{t} \mid \mathbf{a}) = 1
    $$
\end{definition}

\begin{definition}[Conditional Token Prior Function Sequence]
    A conditional token prior function sequence is a sequence of conditional token prior functions,
    $ \{ \mathbf{P}_n \} $.
\end{definition}

\begin{definition}[Token Branch Prior]
    We refer to the prior probability of beginning with a given sequence of tokens as the,
    ``token branch prior'', denoted $\mathbf{P}^{br}_n(\mathbf{a})$. It is defined as,
    $$
    \mathbf{P}^{br}_n(\mathbf{a}) = \prod_{i < |\mathbf{a}|} \mathbf{P}_n(\mathbf{a}[i] \; | \; \mathbf{a}[:i])
    $$
    Once again, we pedantically require calling this a ``branch prior'', to remind ourselves
    that priors stricly only apply to properly terminated strings. Note however, that in the case
    that $\mathbf{a}$ is properly terminated this is the true prior probability of the string (see below).
\end{definition}

Henceforth we will study prior function sequences that are derived from token
prior functions by the relation,
$$
P_n(h) = \mathbf{P}^{br}_n(T(h))
$$

We can now state our first result

\begin{theorem}[Token Prior Path Summation]
    The branch prior of a string $a$, is the sum of the token branch priors of all
    token sequences that pass through $a$.
    $$
    P^{br}(a)=
    \begin{cases}
        1, & a=\epsilon,\\[4pt]
        \sum_{\mathbf{b}\in\mathbf{\Lambda}^*,\,T^{-1}(\mathbf{b})<a}\mathbf{P}^{br}(\mathbf{b})
        \sum_{\mathbf{t}\in\mathbf{\Lambda},\,a\le T^{-1}(\mathbf{b+t})}\mathbf{P}(\mathbf{t}\mid\mathbf{b}), & a\neq\epsilon.
    \end{cases}
    $$
\end{theorem}
\begin{proof}
    We prove the two cases separately.

    If $a=\epsilon$, then by Definition~\ref{def:branch-prior-function},
    $$
    P^{br}(\epsilon)=\sum_{\substack{h\in H\\ h\ge \epsilon}}P(h)=\sum_{h\in H}P(h)=1.
    $$

    Now let $a\neq\epsilon$. Again from Definition~\ref{def:branch-prior-function},
    $$
    P^{br}(a)=\sum_{\substack{h\in H\\ h\ge a}}P(h).
    $$

    For each $h\ge a$, write $\mathbf{c}:=T(h)$. Since $T^{-1}(\mathbf{c})=h\ge a$,
    there exists a unique smallest $j$ such that
    $a\le T^{-1}(\mathbf{c}[:j+1])$.
    Define $\mathbf{b}:=\mathbf{c}[:j]$ and $\mathbf{t}:=\mathbf{c}[j]$.
    By minimality of $j$, we have
    $$
    T^{-1}(\mathbf{b})<a\le T^{-1}(\mathbf{b}+\mathbf{t}).
    $$
    So each $h\ge a$ determines a unique pair $(\mathbf{b},\mathbf{t})$ with those inequalities.

    Grouping the sum over $h$ by these pairs gives
    $$
    P^{br}(a)=
    \sum_{\substack{\mathbf{b}\in\mathbf{\Lambda}^*\\T^{-1}(\mathbf{b})<a}}
    \sum_{\substack{\mathbf{t}\in\mathbf{\Lambda}\\a\le T^{-1}(\mathbf{b}+\mathbf{t})}}
    \sum_{\substack{h\in H:\\ T(h)\text{ begins with }\mathbf{b}+\mathbf{t}}}
    P(h).
    $$

    The innermost sum is exactly the prior probability of beginning with
    $\mathbf{b}+\mathbf{t}$, namely
    $\mathbf{P}^{br}(\mathbf{b}+\mathbf{t})$.
    By the token branch prior definition,
    $$
    \mathbf{P}^{br}(\mathbf{b}+\mathbf{t})
    =\mathbf{P}^{br}(\mathbf{b})\,\mathbf{P}(\mathbf{t}\mid\mathbf{b}).
    $$
    Substituting yields
    $$
    P^{br}(a)=
    \sum_{\mathbf{b}\in\mathbf{\Lambda}^*,\,T^{-1}(\mathbf{b})<a}\mathbf{P}^{br}(\mathbf{b})
    \sum_{\mathbf{t}\in\mathbf{\Lambda},\,a\le T^{-1}(\mathbf{b+t})}\mathbf{P}(\mathbf{t}\mid\mathbf{b}),
    $$
    as claimed.
\end{proof}



\section{Old stuff}
Formally, to convert a token model to a literal model, we take the sum across 
all compatible token sequences $\mathbf{B}$, and all compatible terminal tokens $\mathbf{t}$,
$$
P(S) = \sum_{\mathbf{B} < S} P(\mathbf{B}) \sum_{\mathbf{t},\, S \leq \mathbf{B}+\mathbf{t}} P()
$$

We use two tricks to efficiently compute this sum:
\begin{enumerate}
    \item We recognize that the eligible longest token prefixes correspond to the prefixes of the last word, since tokens never contain spaces in the middle of them.
    Additionally we can limit the values of $\mathbf{B}$ to at most 15 ancestor prefixes, since tokens are at most 15 characters long in our tokenizer.
    \item We can preconvert the language model's output of logits to an array of \textit{cumulative} probabilities.
    This allows us to efficiently compute the alphabetical sum over compatible $\mathbf{t}$, by taking the difference between the cumulative probabilities of the start and end.
\end{enumerate}





\section{Global Variables}

\begin{itemize}
    \item \texttt{track\_mdlfta}: \textbf{bool} --- indicates whether we are tracking maximum descendant likelihoods for token ancestors.
    This is only necessary on the server where we will want to be able to evaluate token sequence posteriors.
    \item \texttt{max\_token\_length}: \textbf{int} --- the maximum length of a token.
    \item \texttt{pta\_lut}: \textbf{dict} --- a lookup table for possible token ancestors, mapping from (ancestor final token, token prefix) to a boolean indicating whether it 
    is possible for any token beginning with the token prefix to follow upon the ancestor final token.
    \item \texttt{llm\_results}: \textbf{dict} --- a mapping from (token sequence, token) to the language model's conditional probability of the token given the token sequence.
    \item \texttt{llm\_fans}: \textbf{dict} --- a mapping from (token sequence, token prefix) to the language model's conditional probability that the next token begins with the token prefix given the token sequence.
\end{itemize}

\section{Fields of a Node}
A node in the trie has the following fields.
(Note: All array fields have length \texttt{max\_token\_length} (0-indexed) unless otherwise specified.)

Constant Fields:
\begin{itemize}
    \item \texttt{letter}: \textbf{str} --- the character represented by this node.
    \item \texttt{value}: \textbf{str} --- the string represented by this node, i.e., the concatenation of the parent's value and the character represented by this node.
    (In implementation, we only need at most the last 2*\texttt{max\_token\_length} characters of the value.)
    \item \texttt{depth}: \textbf{int} --- the depth of this node in the trie.
    \item \texttt{final\_token}: \textbf{str} --- the token to which this node belongs, if this node is the last character of a token.
    \item \texttt{final\_token\_length}: \textbf{int} --- the number of characters in the final token.
    \item \texttt{ancestor\_final\_tokens}: \textbf{list of strs} --- the $i$-th ancestor final token is the final token of the $i$-th ancestor.
     (In implementation, this can be an array of the ancestor final token lengths, and we may slice into our value to derive the final token.)
    \item \texttt{possible\_token\_ancestors}: \textbf{list of bool} --- a list of flags indicating whether the $i$-th ancestor
    could possibly be a token ancestor of this node. (See chapter \ref{chap:determine-most-likely-break-nodes}.)
\end{itemize}

Mutable Fields:
\begin{itemize}
    \item \texttt{posterior\_Z}: \textbf{float} --- the unnormalized posterior probability of this node being a prefix of the user's target string,
    \item \texttt{ever\_parent\_of\_visible\_node}: \textbf{bool} --- indicates whether this node has ever been a parent of a visible node
    (at a time when a signal was received).
    This must be false for the likelihood field to be valid.
    If this flag is true, the likelihood can only be calculated as a weighted sum of its children's likelihoods,
    $P(D | S \leq X) = \sum_l P(D | S+l \leq X) P(S+l \leq X | S \leq X)$,
    since its children have different likelihoods.
    Hence, if a node was not ever a parent of a visible node, then its likelihood is known absolutely,
    but if a node was once a parent of a visible node, then its likelihood is at best known conditional on a prior model.
    Since our prior model mutates over time, this is not definite knowledge of the likelihood.
    % prior
    \item \texttt{prior}: \textbf{float} --- the prior probability of this node being a prefix of the user's target string.
    \item \texttt{break\_prior}: \textbf{float} --- the prior probability that not only is this node a prefix of the user's target string,
    but also that it is the last character of a token in the tokenization of the user's target string.
    \item \texttt{ancestor\_break\_priors}: \textbf{list of floats} --- the $i$-th ancestor break prior is the \texttt{break\_prior} of the $i$-th ancestor. This may be stale.
    \item \texttt{unpushed\_break\_priors}: \textbf{list of optional floats} --- the $i$-th element is the fresh \texttt{break\_prior} if the $i$-th ancestor's \texttt{break\_prior} has changed since the last time we visited this node.
    \item \texttt{token\_fans}: \textbf{list of floats} --- the $i$-th token fan is the prior probability of this node being a prefix of the target string 
    conditional on its $i$-th parent being the last character of the token immediately preceding the token to which this character belongs.
    \item \texttt{final\_token\_prior}: \textbf{float} --- the conditional prior probability of the final token if the ancestor
    at depth \texttt{depth - final\_token\_length} is the last character of a token.
    \item \texttt{unpushed\_llm\_results}: \textbf{list of optional bool} --- the $i$-th element is true if the $\texttt{llm\_result}$ corresponding
    to the value of the $i$-th ancestor has changed since the last time we visited this node.
    % likelihood
    \item \texttt{likelihood}: \textbf{optional float} --- the accumulated likelihood of all signals conditional on this node being a prefix
    of the user's target string, $\prod_{d \in D} P(d | S \leq X)$.
    \item \texttt{unpushed\_likelihood}: \textbf{float} --- likelihood that has not been accumulated to descendant nodes.
    \item \texttt{max\_descendant\_likelihood\_for\_token\_ancestor}: \textbf{list of floats} --- the $i$-th element is the 
    maximum (valid) likelihood of any descendant of this node which could also have this node's $i$-th ancestor as a token ancestor. (See chapter \ref{chap:determine-most-likely-break-nodes}.)
\end{itemize}



\section{Valid Descent}
The above data structure gives us the machinery to effectively defer the calculation of the posteriors of most nodes.

When we visit a node $x$, we update the statistics of its children. If
the node $x$ has no children because it has never been visited before,
then we initialize its children.

\begin{algorithm}[H]
\caption{Initialize Children}
\label{alg:initialize-children}
\begin{algorithmic}[1]
\Require Node $x$
\State $x.\text{unpushed\_likelihood} \gets 1$
\State $x.\text{unpushed\_break\_priors}[i] \gets \text{None} \quad \forall i$
\State $x.\text{unpushed\_llm\_results}[i] \gets \text{None} \quad \forall i$
\For{each character $c$ in the alphabet}
    \State Create child node $y$ of $x$
    % constant fields
    \State $y.\text{letter} \gets c$
    \State $y.\text{value} \gets x.\text{value} + c$
    \State $y.\text{depth} \gets x.\text{depth} + 1$
    \State Tokenize the string corresponding to $y$ into a sequence of tokens $\mathbf{T} = [\mathbf{t}_1, \dots, \mathbf{t}_k]$
    \State $y.\text{final\_token} \gets \mathbf{t}_k$
    \State $y.\text{final\_token\_length} \gets |\mathbf{t}_k|$
    \State $y.\text{ancestor\_final\_tokens}[0] \gets y.\text{final\_token}$
    \State $y.\text{ancestor\_final\_tokens}[i] \gets x.\text{ancestor\_final\_tokens}[i - 1] \quad \forall i \ge 1$
    % mutable fields
    \State $y.\text{ever\_parent\_of\_visible\_node} \gets \text{false}$
    % prior
    \State $y.\text{ancestor\_break\_priors}[i] \gets x.\text{ancestor\_break\_priors}[i-1] \quad \forall i \ge 1$
    \State $y.\text{unpushed\_break\_priors}[i] \gets \text{None} \quad \forall i$
    \State $y.\text{unpushed\_llm\_results}[i] \gets \text{None} \quad \forall i$
    \State $y.\text{final\_token\_prior} \gets \texttt{llm\_results}[(y.\text{value}[:-y.\text{final\_token\_length}],\ y.\text{final\_token})]$
    \State $\begin{aligned}[t]
        y.\text{break\_prior} &\gets y.\text{ancestor\_break\_priors}[y.\text{final\_token\_length}] \\
        &\quad * y.\text{final\_token\_prior}
    \end{aligned}$
    \State $y.\text{ancestor\_break\_priors}[0] \gets y.\text{break\_prior}$
    \State $y.\text{token\_fans}[i] \gets \texttt{llm\_fans}[(y.\text{value}[:-i],\ y.\text{value}[-i:])] \quad \forall i$
    \State $y.\text{prior} \gets \sum_i y.\text{ancestor\_break\_priors}[i] * y.\text{token\_fans}[i]$
    % likelihood
    \State $y.\text{likelihood} \gets x.\text{likelihood}$
    \If{\texttt{track\_mdlfta}}
        \State $y.\text{possible\_token\_ancestors}[i] \gets \texttt{pta\_lut}[y.\text{ancestor\_final\_tokens}[i],\ y.\text{value}[-i:]] \quad \forall i$

        \Comment{This correctly sets $y.\text{possible\_token\_ancestors}[0]$ to true.}
        \State $\begin{aligned}[t]
            &y.\text{max\_descendant\_likelihood\_for\_token\_ancestor}[i] \gets \\
            &\quad \begin{cases} 
                x.\text{likelihood} & \text{if } y.\text{possible\_token\_ancestors}[i] \\ 
                0 & \text{otherwise} 
            \end{cases} \quad \forall i
        \end{aligned}$
        \State $TrackMDLFTA(y)$ Using algorithm \ref{alg:track-mdlfta}.
    \EndIf
    \State $y.\text{unpushed\_likelihood} \gets 1$
    % posterior
    \State $y.\text{posterior\_Z} \gets y.\text{prior} * y.\text{likelihood}$
\EndFor
\State $x.\text{likelihood} \gets \text{None}$
\end{algorithmic}
\end{algorithm}

When we visit a node, we push the unpushed likelihood and the unpushed break priors to its children.
Let $x$ be our current node, and $y$ be a child of $x$.

\begin{algorithm}[H]
\caption{Visit Node}
\label{alg:visit-node}
\begin{algorithmic}[1]
\Require Node $x$
\For{each child $y$ of $x$}
    % save initial values
    \State $\texttt{old\_prior} \gets y.\text{prior}$

    \If{$y.\text{ever\_parent\_of\_visible\_node}$ is \text{false}}
        \State $\texttt{old\_likelihood} \gets y.\text{likelihood}$
        \State $y.\text{likelihood} \mathrel{*=} x.\text{unpushed\_likelihood}$
    \EndIf
    %
    \State $y.\text{unpushed\_likelihood} \mathrel{*=} x.\text{unpushed\_likelihood}$
    \State $y.\text{unpushed\_llm\_results}[i] \mathrel{\lor=} x.\text{unpushed\_llm\_results}[i] \quad \forall i$
    \State $y.\text{unpushed\_break\_priors}[i] \mathrel{\lor=} x.\text{unpushed\_break\_priors}[i] \quad \forall i$
    % refresh token fans
    \State $\begin{aligned}[t]
        y.\text{token\_fans}[i] &\gets \texttt{llm\_fans}[(y.\text{value}[:-i],\ y.\text{value}[-i:])] \\
        &\quad \forall i \text{ s.t. } x.\text{unpushed\_llm\_results}[i] \neq \text{None}
    \end{aligned}$
    % refresh prior
    \State $\texttt{prior\_changed} \gets \left(\exists~i:~x.\text{unpushed\_break\_priors}[i] \neq \text{None}\right) \vee \left(\exists~i:~x.\text{unpushed\_llm\_results}[i] \neq \text{None}\right)$
    \If{$\texttt{prior\_changed}$}
        \State $y.\text{prior} \gets \sum_i y.\text{ancestor\_break\_priors}[i] * y.\text{token\_fans}[i]$
    \EndIf
    % refresh final_token_prior
    \If{$x.\text{unpushed\_llm\_results}[x.\text{final\_token\_length}]$ is not \text{None}}
        \State $y.\text{final\_token\_prior} \gets \text{LLM\_Results}[y.\text{value}[:-y.\text{final\_token\_length}]][y.\text{final\_token}]$
        \State $y.\text{break\_prior} \gets y.\text{ancestor\_break\_priors}[y.\text{final\_token\_length}] * y.\text{final\_token\_prior}$
        \State $y.\text{ancestor\_break\_priors}[0] \gets y.\text{break\_prior}$
        \State $y.\text{unpushed\_break\_priors}[0] \gets y.\text{break\_prior}$
    \EndIf
    % TODO: think hard about how to properly set postZ
    \If{$y.\text{ever\_parent\_of\_visible\_node}$ is \text{false}}
        \State $y.\text{posterior\_Z} \gets y.\text{prior} * y.\text{likelihood}$
    \Else
        \State $\texttt{prior\_correction} \gets \texttt{y.\text{prior}} / \texttt{old\_prior}$
        \State $\texttt{likelihood\_correction} \gets x.\text{unpushed\_likelihood}$
        \State $y.\text{posterior\_Z} \mathrel{*=} \texttt{prior\_correction} * \texttt{likelihood\_correction}$
    \EndIf
    % TODO: think about when max descendant likelihoods is actually handled.
    \If{\texttt{track\_mdlfta} is true and $y.\text{ever\_parent\_of\_visible\_node}$ is false}
        \State $\begin{aligned}[t]
            &y.\text{max\_descendant\_likelihood\_for\_token\_ancestor}[i] \gets \\
            &\quad \begin{cases} 
                y.\text{likelihood} & \text{if } y.\text{possible\_token\_ancestors}[i] \\ 
                0 & \text{otherwise} 
            \end{cases} \quad \forall i
        \end{aligned}$
        \State $TrackMDLFTA(y)$ Using algorithm \ref{alg:track-mdlfta}.
    \EndIf
\EndFor
\State $x.\text{unpushed\_likelihood} \gets 1$
\State $x.\text{unpushed\_break\_priors}[i] \gets \text{None} \quad \forall i$
\State $x.\text{unpushed\_llm\_results}[i] \gets \text{None} \quad \forall i$
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Track Token Descendant Max Likelihood}
\label{alg:track-mdlfta}
\begin{algorithmic}[1]
\Require Node $x$
\Require $x.\text{max\_descendant\_likelihood\_for\_token\_ancestor}[i] \text{ is already correct for all } i$
\For{each $i < \texttt{max\_token\_length}$}
    \State $\texttt{new\_mdlfta} \gets \texttt{x}.\text{max\_descendant\_likelihood\_for\_token\_ancestor}[i]$
    \State $\texttt{node} \gets x$
    \State $\texttt{dist\_to\_last\_token} \gets i$
    \While{true}
        \State $\texttt{old\_mdlfta} \gets \texttt{node}.\text{max\_descendant\_likelihood\_for\_token\_ancestor}[\texttt{dist\_to\_last\_token}]$
        \If{$\texttt{old\_mdlfta} < \texttt{new\_mdlfta}$}
            \State $\texttt{node}.\text{max\_descendant\_likelihood\_for\_token\_ancestor}[\texttt{dist\_to\_last\_token}] \gets \texttt{new\_mdlfta}$
        \Else
            \State break
            \label{alg:track-mdlfta-break}
        \EndIf
        \If{$\texttt{dist\_to\_last\_token} = 0$}
            \State $\texttt{dist\_to\_last\_token} \gets \texttt{node}.\text{final\_token\_length}$
        \EndIf
        \State $\texttt{dist\_to\_last\_token} \mathrel{-=} 1$
        \State $\texttt{node} \gets \texttt{node}.\text{parent}$
        \If{$\texttt{node}$ is \text{None}}
            \State break
        \EndIf
    \EndWhile
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}
\caption{Valid Descent}
\label{alg:valid-descent}
\begin{algorithmic}[1]
\Require Bayesian Trie $B$ and node $x$ in $B$.
\If{$x$ is a leaf in $B$}
    \State InitializeChildren($x$) Using algorithm \ref{alg:initialize-children}.
\Else
    \State VisitNode($x$) Using algorithm \ref{alg:visit-node}.
\EndIf
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Ensure Rooted Subgraph}
\label{alg:ensure-rooted-subgraph}
\begin{algorithmic}[1]
\Require Bayesian Trie $B$, and a trie $V$ which is a subgraph of $B$ sharing the root.
\State Let $Q$ be a queue initialized with the root of $V$.
\While{$Q$ is not empty}
    \State Pop $v$ from $Q$
    \State Let $b$ be the node in $B$ corresponding to $v$
    \State ValidDescent($B$, $b$) Using algorithm \ref{alg:valid-descent}.
    \For{each child $u$ of $v$ in $V$}
        \State Push $u$ to $Q$
    \EndFor
\EndWhile
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Up Propagation}
\label{alg:up-propagation}
\begin{algorithmic}[1]
\Require Bayesian Trie $B$ and rooted subgraph $V$ of $B$.
\State Sort the nodes in $V$ in reverse topological order.
\For{each node $v$ in the reverse topological order}
     \If{$v$ is not a leaf in $V$}
        \State Set $v.\text{post\_Z} \gets \sum_{u \in v.\text{children}} u.\text{post\_Z}$
    \EndIf
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{definition}
    We define the truncation of a token sequence, $\lfloor \mathbf{B} \rfloor_d$,
    to be the longest token sequence prefix of $\mathbf{B}$, which has fewer than $d$ characters,
    i.e., it is the longest $\mathbf{C}$ such that $|Literalize(\mathbf{C})| < d$.
\end{definition}

\begin{definition}
    For a given character sequence, $S$, and
    a given token sequence, $\mathbf{B}$,
    (which are compatible with each other in the sense that $\mathbf{B} \leq S$),
    we define the ``Chain Maximum Likelihood'' with respect to depth $d$,
    to be the maximum likelihood of any descendant which is compatible
    with both the character sequence and the token sequence up to depth $d$.
    $$
    ChainMaxLikelihood(S, \mathbf{B}, d) = \max_{
        \substack{
            Q \geq S[:d] \\
            Tokenize(Q) \geq \lfloor \mathbf{B} \rfloor_d
        }
    } P(D \mid Q)
    $$
\end{definition}

\begin{theorem}
\label{thm:chain-max-likelihood-decreasing}
    For a given character sequence, $S$, and
    a given token sequence, $\mathbf{B}$,
    the Chain Maximum Likelihood is a decreasing function of the depth.
\end{theorem}

\begin{proof}
    The two conditions become more restrictive, i.e.,
    \begin{align*}
        Q \geq S[:d+1] &\implies Q \geq S[:d] \\
        Tokenize(Q) \geq \lfloor \mathbf{B} \rfloor_{d+1} &\implies Tokenize(Q) \geq \lfloor \mathbf{B} \rfloor_d
    \end{align*}
\end{proof}

Theorem \ref{thm:chain-max-likelihood-decreasing}
is necessary to prove that breaking at line \ref{alg:track-mdlfta-break} in algorithm \ref{alg:track-mdlfta} is correct.

\section{Lazy Updates}
The purpose of an update is to assimilate a change to the likelihood or prior
so that the trie remains in a valid state, i.e., the trie will continue to 
yield correct posterior probabilities if explored via the
``valid descent'' algorithm.

An important necessary condition for an update to be valid is that
it correctly updates the unnormalized posterior probability of the root node.

Each update must be performed in four phases:
\begin{enumerate}
    \item Select intial frontier: we choose a set of nodes from which to begin the algorithm.
    \item Down Propagation: 
    we descend the trie until we reach a ``valid update leafset'' of nodes, i.e., a set of nodes
    which is a complete prefix partition and for which we can know the unnormalized posteriors of each node.
    Efficiency demands that we recognize once we have reached a valid leafset and stop descending as early as possible.
    \item Fix Leafset: we fix the unnormalized posteriors
    of all the nodes in the valid update leafset.
    \item Up Propagation: 
    we ascend the trie, accumulating unnormalized posteriors
\end{enumerate}

The trick is to choose the smallest prefix partition for which we can truly calculate the unnormalized posteriors.
The unnormalized posterior is a sum over one's children's unnormalized posteriors:

How do we know when we have reached a valid update leafset?
We assume that after receiving an update, \texttt{posterior\_Z},
that is, $P_0(S | D)$,
has been invalidated for every node in the trie. 
We hope to derive $P_1(S | D)$, without summing over the children's posteriors,
$$
P_1(S|D) = \sum_i P_1(S_i | D).
$$

In general if one term is reweighted, we will have to recalculate,
but there are four cases in which we can avoid recalculation and descent:
\begin{enumerate}
    \item The likelihood (and prior) of $S$ is known explicitly,
    \begin{align}
        P_1(S | D) &= P_1(S) P_1(D | S)
    \end{align}
    \item Neither term (neither likelihood nor prior) changes.
    \begin{align}
        P_1(S | D) &= P_0(S | D)
    \end{align}
    \item The likelihoods change by a constant factor $l$, and the priors do not change.
    \begin{align}
        P_1(S|D) 
            &= \sum_i P_1(S_i | D) \notag \\
            &= \sum_i P_1(S_i) P_1(D | S_i) \notag \\
            &= \sum_i P_1(S_i) \big(l \cdot P_0(D | S_i)\big) \notag \\
            &= l \sum_i P_0(S_i) P_0(D | S_i) \notag \\
            &= l \cdot P_0(S | D)
    \end{align}
    \item The priors change by a constant factor $p$, and the likelihoods do not change,
    \begin{align}
        P_1(S|D) 
            &= \sum_i P_1(S_i | D) \notag \\
            &= \sum_i P_1(S_i) P_0(D | S_i) \notag \\
            &= \sum_i \big(p \cdot P_0(S_i)\big) P_0(D | S_i) \notag \\
            &= p \sum_i P_0(S_i) P_0(D | S_i) \notag \\
            &= p \cdot P_0(S | D)
    \end{align}
\end{enumerate}
In order to find the smallest prefix partition for the Up Propagation step,
we consider the set of all nodes that satisfy any of these three cases and
then take the minimal elements of this set (resulting in an antichain / frontier of the tree).




% !TEX root = ../math.tex
\section{Likelihood Update}
\label{sec:likelihood-update}

After receiving a likelihood result from the user interface, we must update the trie to increase the
posterior probabilities of the nodes most likely indicated by the user's signal.

\textbf{Step 1: Select initial frontier:}
We choose the set
of all nodes that were leaves in the visible trie,
along with all invisible nodes that have a visible sibling.
Equivalently, we are choosing the minimal elements of the set of nodes where
every node has no visible children, i.e., has a valid likelihood for this observation, $D_i$.
(We should descend from the root to gather these nodes to ensure that
unpushed priors have been updated correctly, i.e., to ensure that every
node in this set has a correctly calculated prior.)

\textbf{Step 2: Down Propagation:}
Our initially selected frontier is actually a valid update leafset,
so no down propagation is necessary.
Our initially selected frontier is a valid leafset because
it satisfies case 3 above:
the likelihood of a given node in this set may not be known explicitly
(since it may have been the parent to visible children at some time),
but we know how its implicit (true) likelihood has changed and we known that
its prior has not changed.

\textbf{Step 3: Fix Leafset:}
We fix the unnormalized posteriors of all the nodes in the valid update leafset.
We guarantee a valid unnormalized posterior by setting the \texttt{post\_Z} of each node to,
$$
x.\text{post\_Z} = x.\text{post\_Z} * \text{new\_likelihoods[x.value]}
$$

\textbf{Step 4: Up Propagation:}
We ascend the trie, accumulating unnormalized posteriors of the parents as
$$
x.\text{post\_Z} = \sum_{y \in x.\text{children}} y.\text{post\_Z}
$$

\begin{algorithm}[H]
\caption{Complete Visible Trie}
\label{alg:complete-visible-trie}
\begin{algorithmic}[1]
\Require{Trie $V$ of visible nodes}
\For{each node $v \in V$}
    \If{$v$ is a leaf in $V$}
        \State \textbf{continue} \Comment{Skip if it is already a leaf}
    \EndIf
    \Comment{Propagate likelihood to invisible children}
    \For{each character $c$ in the alphabet}
        \If{$(v + c) \notin V$}
            \State Add $(v + c)$ to $V$
            \State Set $(v + c).\text{new\_likelihood} \gets v.\text{new\_likelihood}$
        \EndIf
    \EndFor
\EndFor
\Return $V$
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Likelihood Update}
\label{alg:likelihood-update}
\begin{algorithmic}[1]
\Require{Bayesian Trie $B$, trie of visible nodes, $V$, with likelihoods for latest observation.}
\State $V \gets \text{CompleteVisibleTrie}(V)$ Using algorithm \ref{alg:complete-visible-trie}.
\State EnsureRootedSubgraph($B$, $V$) Using algorithm \ref{alg:ensure-rooted-subgraph}.
\For{each node $v \in V$}
    \State Set $b$ to be the node in the bayesian trie corresponding to $v$
    \If{$v$ is a leaf in $V$}
        \State $b.\text{likelihood} \mathrel{*=} v.\text{new\_likelihood}$
        \State $b.\text{post\_Z} \mathrel{*=} v.\text{new\_likelihood}$
        \If{\texttt{track\_mdlfta}}
            \State $b.\text{max\_descendant\_likelihood\_for\_token\_ancestor}[i] \mathrel{*=} v.\text{new\_likelihood} \quad \forall i$
            \State $TrackMDLFTA(b)$ Using algorithm \ref{alg:track-mdlfta}.
        \EndIf
    \EndIf
\EndFor
\State UpPropagation($B$, $V$) Using algorithm \ref{alg:up-propagation}.
\end{algorithmic}
\end{algorithm}


\subsubsection{Maintaining maximum descendant likelihoods}
For every node in our update leafset we factor in the new likelihood not only to the likelihood of the given node, but also to all elements of the node's array
\texttt{max\_descendant\_likelihood\_for\_token\_ancestor} as well as the scalar, \texttt{max\_token\_descendant\_likelihood}.
when we visit a node during up propagation we immediately accumulate these quantities in the node's possible ancestors.

\subsubsection{Possible Token Ancestors}
A possible token ancestor of a character sequence, $S$, is a token sequence, $\mathbf{A}$, such that there is some string $Y$, such
that $S$ is a prefix of $Y$, $S \leq Y$, and $\mathbf{A}$ is a token sequence prefix of the full tokenization of $Y$, and is the longest
such prefix for which $\mathbf{A} \leq S$. In other words, it is possible that the last token of $\mathbf{A}$ is the last complete token in the tokenization of $Y$ before
the last character of $S$.

A necessary condition is that $(S - \mathbf{A})$ is a character sequence prefix of some token $\mathbf{t}$.
Designating the last token of $\mathbf{A}$ as $\mathbf{s} = \mathbf{A}[-1]$, the necessary and sufficient condition
is that there exists some token $\mathbf{t}$ such that $(S - \mathbf{A})$ is a prefix of $\mathbf{t}$, 
and tokenizing the string formed by concatenating $\mathbf{s}$ and $\mathbf{t}$ yields exactly the two tokens $\mathbf{s}$ and $\mathbf{t}$ in order,
i.e., \texttt{Tokenize}$(\mathbf{s} + \mathbf{t}) = [\mathbf{s}, \mathbf{t}]$.

Evaluating this predicate for given $\mathbf{s}$ and $(S - \mathbf{A})$ has resisted my attempts to find an efficient algorithm.
However, it can be solved in $O(1)$ time by precomputing a lookup table for all possible pairs of tokens, $\mathbf{s}$, and
token prefixes, $(S - \mathbf{A})$. This table has size $O(V^2)$, where $V$ is the vocabulary size.
If the vocabulary size is $50,\!000$ and we require one bit of memory per entry, this amounts to only about $300$MB.

Interestingly for a random choice of $\mathbf{s}$ and $\mathbf{t}$, the probability that $\mathbf{s} + \mathbf{t}$ tokenizes to $[\mathbf{s}, \mathbf{t}]$ is about $1/2$.
Even knowing $\mathbf{s}$ does not help very much: the conditional entropy is still about $0.6$ bits.
This result holds when we restrict $\mathbf{t}$ to tokens that are not prefixes of other tokens. Since a substantial
fraction of the possible values for $(S - \mathbf{A})$ will be of this form, this indicates that we cannot effectively
make a compression of the lookup table.

However, since this is only needed for the step of determining the most likely break nodes, which occurs on the server,
we can afford to use a lookup table, since the server has to keep a language model in memory anyway.




% !TEX root = ../math.tex
\section{Prior Update}
\label{sec:prior-update}

After receiving an ``LLM Result'' we must update the trie to reflect this refinement of
the language model.

\textbf{Step 1: Select initial frontier:}
The ``LLM Result'' corresponds to a token sequence, $\mathbf{A}$.
We choose the corresponding character sequence, $S$, in the bayesian trie as our initial frontier.

\textbf{Step 2: Down Propagation:}
We descend from the initial frontier via valid descent until we reach a valid update leafset.
A node is known to belong to the valid update leafset, and no further descent from that
node is necessary, if and only if the node satisfies one of the following two cases:
\begin{itemize}
    \item \textbf{Valid Likelihood}: The node has never been a parent to a visible node, i.e., it has a valid likelihood.
    \item \textbf{Space Character}: The node is a space character. Formally, the condition is that there exists
    some token following upon $\mathbf{A}$ which every possible suffix of our node $S$, must contain, i.e.,
    we require that $S$ satisfy,
    $$
    \exists \mathbf{B}, \mathbf{B}>\mathbf{A}, |\mathbf{B}|=|\mathbf{A}|+1, \forall Y \geq S, \mathbf{B} < Tokenize(Y)
    $$
    but in practice we implement this by recognizing that this condition is always true of the space
    character and rarely true otherwise.
\end{itemize}

\textbf{Step 3: Fix Leafset:}
How we fix the unnormalized posterior of an update leafset node depends on which of the 
two cases above it satisfies.
If it has a valid likelihood, then the likelihood and prior are known 
explicitly (case 1 above) and we can set the \texttt{post\_Z} to,
$$
S.\text{post\_Z} = S.\text{prior} * S.\text{likelihood}
$$
If it is a space character, then we know that this node's prior and the priors of all
its descendants must pass through the same token sequence, $\mathbf{B} > \mathbf{A}$.
For this node and for all descendants $Y$, we have,
$$
P_1(Y) = P_0(Y) * \frac{P_1(\mathbf{B}|\mathbf{A})}{P_0(\mathbf{B}|\mathbf{A})}
$$
since this is the only factor in the product of conditional probabilities that has changed.
We can determine the relevant predicted token as, $\texttt{shared\_token} = Tokenize(S)[|\mathbf{A}|]$ and so the correction factor
is,
$$
\texttt{correction\_factor} = \frac{P_1(\mathbf{B}|\mathbf{A})}{P_0(\mathbf{B}|\mathbf{A})} = \frac{\text{LLM\_Result}[\texttt{shared\_token}]}{\text{Old\_LLM\_Result}[\texttt{shared\_token}]}
$$
and we can set the \texttt{post\_Z} to,
$$
S.\text{post\_Z} = S.\text{post\_Z} * \texttt{correction\_factor}
$$

\textbf{Step 4: Up Propagation:}
We ascend the trie, accumulating unnormalized posteriors of the parents as
$$
S.\text{post\_Z} = \sum_{Y \in S.\text{children}} Y.\text{post\_Z}
$$

\begin{algorithm}[H]
\caption{Prior Update}
\label{alg:prior-update}
\begin{algorithmic}[1]
\Require Bayesian Trie $B$, Node $S$ (corresponding to token sequence $\mathbf{A}$), $\text{New\_LLM\_Result}$, $\text{Old\_LLM\_Result}$
\State Let $Q$ be a queue initialized with $S$
\State Let $L$ be an empty list \Comment{Valid update leafset}
\Comment{\textbf{Step 2: Down Propagation}}
\While{$Q$ is not empty}
    \State Pop node $x$ from $Q$
    \State ValidDescent($B$, $x$) Using algorithm \ref{alg:valid-descent}.
    \If{$x.\text{ever\_parent\_of\_visible\_node}$ is \text{false} $\lor$ $x.\text{letter}$ is a space character}
        \State Add $x$ to $L$
    \Else
        \For{each child $y$ of $x$}
            \State Push $y$ to $Q$
        \EndFor
    \EndIf
\EndWhile
\Comment{\textbf{Step 3: Fix Leafset}}
\For{each node $x \in L$}
    \If{$x.\text{ever\_parent\_of\_visible\_node}$ is \text{false}}
        \State $x.\text{post\_Z} \gets x.\text{prior} * x.\text{likelihood}$
    \Else \Comment{$x.\text{letter}$ is a space character}
        \State $\texttt{shared\_token} \gets Tokenize(x.\text{value})[|\mathbf{A}|]$
        \State $\texttt{correction\_factor} \gets \frac{\text{New\_LLM\_Result}[\texttt{shared\_token}]}{\text{Old\_LLM\_Result}[\texttt{shared\_token}]}$
        \State $x.\text{post\_Z} \mathrel{*=} \texttt{correction\_factor}$
    \EndIf
\EndFor
\Comment{\textbf{Step 4: Up Propagation}}
\State Let $V$ be the subgraph of $B$ formed by the paths from the root to every node in $L$
\State UpPropagation($B$, $V$) Using algorithm \ref{alg:up-propagation}.
\end{algorithmic}
\end{algorithm}

\textbf{Note Bene:} Why is descending to the space character necessary? After all,
if we know exactly how a node's prior has changed, and we know that its likelihood has not changed,
then we should be able to correctly recalculate the unnormalized posterior, just as how 
in the likelihood update we could recalculate the unnormalized posterior of a node
if we knew how its likelihood had changed and that its prior had remained constant.
The subtle fallacy in this thinking is that the \textit{likelihood of the node has changed}.
Although we often think of the likelihood as being independent of the prior,
this is only true for pure hypotheses. For a composite hypothesis, like this node we have,
$$
P(D | S) = \sum_i P(D | S_i) P(S_i | S)
$$
where $S_i$ are the children of $S$.
Since the weighting, i.e., the prior, has changed, so has the \textit{likelihood}
of this node.
Thus we do not satisfy case 4 above, because the likelihood has changed.


\bigskip
We update the posterior probabilities in two ways:
\begin{enumerate}
    \item \textbf{Likelihood updates}: when the user makes a gesture, the likelihoods of the prefixes are updated. This is a traditional update.
    \item \textbf{Prior updates}: when the language model server returns new or better prior probabilities, we must recalculate the posteriors.
\end{enumerate}

\begin{table}[h]
\centering
\begin{tabular}{lll}
\hline
\textbf{} & \textbf{Likelihood Update} & \textbf{Prior Update} \\
\hline
Mathematical Term       & $P(D \mid X)$                         & $P(X)$ \\
Recompute Posteriors    & Yes                                   & Yes \\
Resize Boxes            & Yes                                   & Yes \\
How Many New Nodes?     & Dozens                                & Zero to a few \\
How to Set Phases       & Optimized / gradient descent          & Random (or ancestor / minimum placement) \\
Human-Initiated         & Yes                                   & No \\
Show Immediately        & Yes                                   & No --- wait for ancestor to finish \\
Branch of Tree Affected & Entire tree                           & Single branch \\
\hline
\end{tabular}
\caption{Comparison of likelihood updates and prior updates.}
\label{tab:updates}
\end{table}
