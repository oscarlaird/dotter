[package]
name = "bpe"
version = "0.1.0"
edition = "2024"

[dependencies]
rolling_hash = { path = "../rolling_hash" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
