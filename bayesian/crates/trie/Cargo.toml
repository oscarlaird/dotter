[package]
name = "trie"
version = "0.1.0"
edition = "2024"

[features]
default = ["tokentrie", "proof"]
trie-trace = []
tokentrie = []
proof = []
safefloat = []

[dependencies]
bpe = { path = "../bpe" }
rolling_hash = { path = "../rolling_hash" }

[lib]
path = "src/lib.rs"
